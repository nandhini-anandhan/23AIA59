# Auth token sent as `Authorization: Bearer <token>` to the protected
# /evaluation-service/logs route. Replace with your actual token, or update
# logger-package/src/logger.ts if the server expects a different auth scheme.
VITE_API_AUTH_TOKEN=your-token-here
