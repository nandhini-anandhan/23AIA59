{
  "name": "log-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that posts structured logs to the evaluation test server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
